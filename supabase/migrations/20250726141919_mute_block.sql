/*
  # メニュープランテーブル構造の更新

  1. 変更内容
    - 各料理を個別の日付で保存するように変更
    - 期間重複時の更新機能に対応
    - generated_date を実際の予定日として使用

  2. 既存データの保持
    - 既存のメニューデータは保持される
    - 新しい構造に対応

  3. セキュリティ
    - 既存のRLSポリシーを維持
*/

-- テーブル構造は既存のものを使用（変更不要）
-- generated_date フィールドを実際の料理予定日として使用

-- 既存のRLSポリシーとトリガーはそのまま維持